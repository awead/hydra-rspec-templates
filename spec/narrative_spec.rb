# frozen_string_literal: true
describe "the thing" do
  context "when first created" do
    specify "has no name"

    describe "its default values" do
      it "includes the person who's creating it"
      it "contains this"
      it "contains that"
    end
  end
end
